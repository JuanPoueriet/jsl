## 2026-01-30 - Accessible Search Overlay
**Learning:** Modal components like Search Overlay require explicit `role="dialog"`, `aria-modal="true"`, and accessible labels for the container and close button to be usable by screen readers.
**Action:** Ensure all future modals implement these attributes and use semantic lists (`ul`/`li`) for result sets.
